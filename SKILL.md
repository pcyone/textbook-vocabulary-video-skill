---
name: textbook-vocabulary-video
description: 将已确认的教材词汇分期规划制作为 Remotion 竖屏教学视频和视频号发布文案；支持指定 EP 范围续做、配音缓存恢复、逐期验收和交付。用于教材词汇精讲，不替代每日单词模板、数字人或通用宣传片流程。
---

# 教材词汇自动化视频

复用 `vocabulary-microcourse-v1`：校园情境插画、中英分段配音、词形与搭配、例句跟读、三秒小测。默认交付指定期数的 MP4、SRT、逐期及汇总发布文案；不是仅生成脚本或预览。

## 先定位，再续做

- 从用户指定项目、当前目录或 `VOCABULARY_VIDEO_PROJECT` 找到 `scripts/voice-microcourse.mjs` 所在项目。原工作流使用 `knowledge-paper-template`，不要依赖某个用户名的绝对路径。
- 用期号、单元、文件名定位分期规划和已完成结果，只读相关条目。必须确认教材出版社、年级、册次、版次；本案例为上海教育出版社七上 2024 新版 Unit 2 School life，不套用旧版 Daily life。
- 只完成用户指定范围。已通过验收的期数不重复收费生成、不改动。缺少具体词表或规划时询问缺失内容，不凭期号猜词。
- 遵循已确认画风、配音、比例。既有批准可以复用，不每批重问；新系列没有样片批准时先交付一条样片，批准后再批量。
- 没有项目时用 [运行说明](references/runtime.md) 初始化随附的最小模板；不要在已有工程上覆盖安装。

## 制作

1. 阅读 [内容约定](references/content.md)。将规划转换为可渲染 lesson JSON，人工核对教学内容后才标记 reviewed。规划中的草稿场景和混合语言 narration 不能直接送给渲染器。
2. 按情景生成配图，调用环境中可用的图像生成工具。先查看本地参考图；每期一幅无字的 4:3 情境图，复用水粉、彩铅、纸面质感；中文、英文和音标由模板叠加。复制最终素材到项目 `public/microcourse/assets/`，不能只引用临时生成目录。未经同意不改画风或用 SVG/占位图代替插画。
3. 使用 [运行说明](references/runtime.md) 的预检和明确 slug 列表，依次生成配音、渲染、音频 QA。所有渲染串行执行：共享 `build/microcourse`，不能并行启动多个渲染器。网络配音、浏览器和受限目录操作遵守环境审批。
4. 逐期查看 `storyboard.jpg`，必要时查看单帧。自动排版 PASS 不代表人工视觉验收；检查字形、断行、图片裁切、选项、答案和下期预告。修正文案或画面后，重建该期配音时间线（语音缓存可复用）和视频。
5. 阅读 [验收与交付](references/acceptance.md)，运行发布门禁，交付全部请求期数。制作失败不得标记完成；保留已通过的期数，说明剩余阻塞。

## 不可省略的约束

- 默认 9:16、1080×1920、30 fps、45–70 秒、九场景；长度以真实音频计算，超时先精简讲解，不强行加速。改为横屏或其他教学模式要另行适配，不能声称本模板直接支持。
- 中文和英文分 cue、分语言配音。主词正常一次、慢速一次；例句正常语速。避免独立朗读易歧义词，如动词 present 可用 `present our work`。不要仅凭 IPA 宣称 TTS 为某种口音。
- 问题朗读结束后至少留三秒，之后答案配音和高亮同时出现。所有音频、字幕、场景使用同一帧时间线。
- `u2-21-presentation` 等 slug 带单元前缀，避免不同单元在共享 public 目录相互覆盖。
- 不自动发布到视频号、不新增后续期数、不创建定时任务。只有明确要求才打 ZIP；只有明确要求才上传到 GitHub。
- 密钥留在环境变量或本地忽略文件，不打印、提交或写进 Skill。教材原件、音视频、生成配图和第三方字体/音乐不随 Skill 上传。

## 恢复和停止

同一期内容变化即使只是屏幕文字，也要重建 source hash 对应的时间线。语音缓存按实际请求参数复用。网络故障最多重试一次；鉴权/余额/权限错误立即停止对应步骤并报告，不换供应商、不偷偷降低质量。布局失败只修该期，不改已批准的其他成片。完成视频还必须通过交付检查，不能把 `READY`、导出 100% 或预览当作最终完成。

## 可用资源

- `scripts/workflow.mjs`：预检、验证、按指定 slug 交付；不调用付费 API。
- `scripts/init-project.mjs`：在新目录复制随附最小运行模板，不安装依赖、不覆盖工程。
- `assets/runtime/`：原工作流的 Remotion 组件、配音/渲染/QA 代码及依赖锁；仅做可移植性修正。
- `assets/lesson.example.json`：待审核示例，用来理解字段，不是已经批准的正式教材。

最终给出实际成片和文案链接、检查结果及未完成项。默认中文，隐藏不必要的实现细节。
